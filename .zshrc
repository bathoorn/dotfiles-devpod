# Which plugins would you like to load?
# Standard plugins can be found in $ZSH/plugins/
# Custom plugins may be added to $ZSH_CUSTOM/plugins/
# Example format: plugins=(rails git textmate ruby lighthouse)
# Add wisely, as too many plugins slow down shell startup.
plugins=(git)


# The next line updates PATH for the Google Cloud SDK.
# if [ -f '/Users/ronnie/google-cloud-sdk/path.zsh.inc' ]; then . '/Users/ronnie/google-cloud-sdk/path.zsh.inc'; fi

# The next line enables shell command completion for gcloud.
# if [ -f '/Users/ronnie/google-cloud-sdk/completion.zsh.inc' ]; then . '/Users/ronnie/google-cloud-sdk/completion.zsh.inc'; fi


# . "$HOME/.local/bin/env"
